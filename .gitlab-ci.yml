image: node:lts

pages:
  stage: deploy

  before_script:
    - corepack enable
    - pnpm install --frozen-lockfile

  script:
    - pnpm build
    - mv dist public

  artifacts:
    paths:
      - public

  only:
    - main